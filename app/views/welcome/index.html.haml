%h1 Welcome#index

%p Find me in app/views/welcome/index.html.haml

%p
  - if current_user
    = link_to "Sign out", signout_path
    = current_user.name
    = link_to "View games", tables_path
    / = current_user.inspect
  - else
    = link_to "Sign in with Google", "/auth/google_login"
